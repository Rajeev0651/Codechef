#include <stdio.h>
#include <stdlib.h>
typedef struct NODE
{
	char cell = 'w';
	struct NODE *N,*S,*E,*W;
}
int main()
{
	int T;
	scanf("%d",&T);
	while(T--)
	{
		long long int N,M,i,j,r,c,count=0;
		long int k;
		scanf("%lld%lld%ld",&N,&M,&k);
		for(i=0;i<N;i++)
		{
			for(j=0;j<M;j++)
			{
				if(i==0)
				{
					
				}
			}
		}
	}
}
